wxPython
# Accessible wx.html2.WebView surfaces for the opt-in rich full-text reader
# (renders links, embedded videos, and social posts; reads in NVDA/JAWS).
wx-accessible-webview>=0.2.0
# Bridges ALT out of the focused WebView to open the native menu bar (WebView2
# swallows ALT before wx sees it), so menus work while reading the rich view.
wx-accessible-menubar>=0.1.1
pywin32; sys_platform == "win32"
# Screen-reader speech + Braille output for key-event announcements (issue #67).
accessible_output2
feedparser
requests
beautifulsoup4
yt-dlp[default]
python-dateutil
mutagen
python-vlc
pychromecast>=14.0.0
async-upnp-client
pyatv
trafilatura>=2.1.0
# Structured-metadata extraction (JSON-LD/microdata/OpenGraph) for article
# author/tags/section enrichment used by Filter Rules categorization.
extruct
webrtcvad-wheels
# Pin setuptools to avoid pkg_resources deprecation warnings until upstream drops pkg_resources.
setuptools<81
pytest
pyinstaller
packaging
brotli
# Browser TLS/JA3 impersonation fallback for anti-bot feeds (issue #29)
curl_cffi
html5lib
lxml
