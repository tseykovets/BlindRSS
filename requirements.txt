wxPython
# Accessible wx.html2.WebView surfaces for the opt-in rich full-text reader
# (renders links, embedded videos, and social posts; reads in NVDA/JAWS).
wx-accessible-webview>=0.2.0
pywin32; sys_platform == "win32"
# Screen-reader speech + Braille output for key-event announcements (issue #67).
accessible_output2
feedparser
requests
beautifulsoup4
yt-dlp[default]
python-dateutil
mutagen
python-vlc
pychromecast>=14.0.0
async-upnp-client
pyatv
trafilatura>=2.1.0
# Structured-metadata extraction (JSON-LD/microdata/OpenGraph) for article
# author/tags/section enrichment used by Filter Rules categorization.
extruct
webrtcvad-wheels
# Security floor (pip-audit 2026-07-17): setuptools <83 carries PYSEC-2026-3447.
# The old <81 pin only silenced pkg_resources deprecation warnings; security wins.
setuptools>=83
pytest>=9.0.3
pyinstaller
packaging
brotli
# Browser TLS/JA3 impersonation fallback for anti-bot feeds (issue #29)
curl_cffi
# Fully automated invisible-browser fallback after ordinary feed fetches fail.
# SeleniumBase UC/CDP headless2 is lazy: successful refreshes never start it.
seleniumbase==4.51.3
html5lib
lxml

# --- Security floors (pip-audit, 2026-07-17) ---------------------------------
# Mostly transitive dependencies; the floors force pip to take the patched
# releases. Re-run `python -m pip_audit --local` in the build venv after
# changing any of these.
requests>=2.33.0
urllib3>=2.7.0
idna>=3.15
aiohttp>=3.14.1
cryptography>=48.0.1
zeroconf>=0.149.16
protobuf>=6.33.5
soupsieve>=2.8.4
pygments>=2.20.0
tinytag>=2.2.1
lxml-html-clean>=0.4.5
